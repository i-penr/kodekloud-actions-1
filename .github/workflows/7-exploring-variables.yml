name: Exploring Variables

on:
  push

# Bad approach. Multiple instances of dockerUsername, and password.
# We can use variables at different levels of the workflow to achieve this
#jobs:
#   docker:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Docker Build
#         run: docker build -t docker.io/dockerUsername/imageName:latest
# 
#       - name: Docker Login
#         run: docker login --username=dockerUsername --password=s3cUrePaSsW0rd
# 
#       - name: Docker Publish
#         run: docker push docker.io/dockerUsername/imageName:latest
#   
#   deploy:
#     needs: docker
#     runs-on: ubuntu-latest
#     steps:
#       - name: Docker run
#         run: docker run -d -p 8080:80 docker.io/dockerUsername/imageName:latest


######## Environment variables at the STEP level ########

#jobs:
#  docker:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Docker Build
#        env:
#        # Fake values
#          CONTAINER_REGISTRY: docker.io
#          DOCKER_USERNAME: i-penr
#          IMAGE_NAME: github-actions-nginx
#        # $VARIABLE or ${{ env.VARIABLE }} can be used.
#        run: echo docker build -t ${{ env.CONTAINER_REGISTRY }}/$DOCKER_USERNAME/$IMAGE_NAME:latest
#    
#      - name: Docker Login
#        env:
#          DOCKER_USERNAME: i-penr
#          # Obviously, not secure at all. It's better to use secrets
#          DOCKER_PASSWORD: s3cUrePaSsW0rd
#        run: echo docker login --username=$DOCKER_USERNAME --password=$DOCKER_PASSWORD
      
#   deploy:
#     needs: docker
#     runs-on: ubuntu-latest
#     steps:
#       - name: Docker run
#     env:
#     # Fake values
#       CONTAINER_REGISTRY: docker.io
#       DOCKER_USERNAME: i-penr
#       IMAGE_NAME: github-actions-nginx
#         run: echo docker run -d -p 8080:80 $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest


######## Environment variables at the JOB level ########

#jobs:
#  docker:
#    runs-on: ubuntu-latest
#    env:
#    # Fake values
#      CONTAINER_REGISTRY: docker.io
#      DOCKER_USERNAME: i-penr
#      IMAGE_NAME: github-actions-nginx
#    steps:
#      - name: Docker Build
#        run: echo docker build -t ${{ env.CONTAINER_REGISTRY }}/$DOCKER_USERNAME/$IMAGE_NAME:latest
#    
#      - name: Docker Login
#        env:
#          DOCKER_PASSWORD: s3cUrePaSsW0rd
#        run: echo docker login --username=$DOCKER_USERNAME --password=$DOCKER_PASSWORD
      
#   deploy:
#     needs: docker
#     runs-on: ubuntu-latest
#     env:
#     # Fake values
#       CONTAINER_REGISTRY: docker.io
#       DOCKER_USERNAME: i-penr
#       IMAGE_NAME: github-actions-nginx
#     steps:
#       - name: Docker run
#         run: echo docker run -d -p 8080:80 $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest


######## Environment values at the ROOT level ########

env:
  CONTAINER_REGISTRY: docker.io
  DOCKER_USERNAME: i-penr
  IMAGE_NAME: github-actions-nginx

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Docker Build
        run: echo docker build -t ${{ env.CONTAINER_REGISTRY }}/$DOCKER_USERNAME/$IMAGE_NAME:latest
    
      - name: Docker Login
        env:
          DOCKER_PASSWORD: s3cUrePaSsW0rd
        run: echo docker login --username=$DOCKER_USERNAME --password=$DOCKER_PASSWORD
    
  deploy:
    needs: docker
    runs-on: ubuntu-latest
    steps:
      - name: Docker run
        run: echo docker run -d -p 8080:80 $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest
